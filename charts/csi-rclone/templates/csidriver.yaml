apiVersion: storage.k8s.io/v1
kind: CSIDriver
metadata:
  # PersistentVolumes name the driver, so it cannot change per release.
  name: csi-rclone
  labels:
    {{- include "csi-rclone.labels" . | nindent 4 }}
spec:
  attachRequired: true
  podInfoOnMount: true
